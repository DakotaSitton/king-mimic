node_modules/
.bun/
bun.lockb
*.log
.DS_Store
.claude/
# generated screenshot artifacts (regenerate via tools/shoot.ps1)
tools/shots/

# runtime telemetry event log
telemetry.jsonl

# combat-log debug dump (appended on loss; regenerate by playing)
combatlog.txt

# scratch one-off logic probes / checks (regenerate as needed — never part of a commit)
probe_*.mjs
_*.mjs
_*.json
tools/tunnel.out

# rejected agent-designed card sets — NOT loaded by the engine, must never be committed
content-tank.js
content-summon.js
content-misc.js

# scratch run outputs / one-off tools (see HANDOFF Landmines — never commit)
loop-report.json
tools/mp-playtest.mjs
tools/wear-shot.mjs

# ---- 2026-07-19 cleanup: runtime/deploy noise + untracked-by-design harnesses ----
# server/deploy stdout+stderr logs (verified: no tracked *.out / *.err at add time)
*.out
*.err
# playtest screenshot dumps + scratch artifacts
public/playtest-shots-*/
artifacts/
scratchpad-*.md
# harnesses CLAUDE.md declares untracked BY DESIGN — never git add, never delete
tools/tap-probe.mjs
tools/tier-sim.mjs
tools/tier-sim-results.json
